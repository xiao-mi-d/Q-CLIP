# PE App Notes

Q-CLIP only uses the PE encoder implementation under `model/core/vision_encoder`.

The original PE repository also contains demo and benchmark applications. Those files are not required for Q-CLIP training or evaluation, so they are intentionally omitted from this cleaned release. This directory is retained only to document the upstream PE application boundary.
